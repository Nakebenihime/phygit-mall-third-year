package org.pds.authenticator;

import org.junit.*;
import static org.junit.Assert.*;
import org.pds.security.cryptography.CustomKeyStore;
import org.pds.security.cryptography.AES;

public class CustomKeyStoreTest {
    /*private final String key = CustomKeyStore.getSecretKeyAES();
    private final File file;

    @BeforeClass
    public final void testCheckFileExists(){
        assertTrue("Le fichier existe",file.exists());
    }

    @BeforeClass
    public final void testFileEmpty(){
        assertEquals(0, file.length())
    }

    @Test
    public final void testFileNotEmpty(){
        assertNotNull("Le fichier n'est pas vide", file.length()>0);
    }*/
}